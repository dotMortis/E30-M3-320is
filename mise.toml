[tools]
node = "26.5.0"
uv = "0.12.0"
# Only needed to rebuild hardware/voice-remote/bridge's prebuilt binaries
# (see hardware/voice-remote/PLAN.md) - not required for the vault or the
# Obsidian plugins themselves.
go = "1.26.5"
